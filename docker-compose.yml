services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      REDIS_HOST: redis
    depends_on:
      - redis
